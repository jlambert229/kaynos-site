node_modules
dist
.netlify
.env
.env.local
.DS_Store
*.log
tests/e2e/results
tests/e2e/report
test-results
playwright-report
.swarm/
deno.lock

# Local worktree directories (used for parallel-agent workflows;
# each worktree has its own .git pointer so its contents are tracked
# in its own checkout, but the directory itself shouldn't appear as
# untracked in the parent repo).
.worktrees/
